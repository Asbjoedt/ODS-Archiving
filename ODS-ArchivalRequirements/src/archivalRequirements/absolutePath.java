package archivalRequirements;

public class absolutePath {

    // Check for absolute path to local directory
    public boolean Check_ODFToolkit(String filepath) {
        boolean absPath = false;

        return  absPath;
    }

    // Remove absolute path to local directory
    public boolean Change_ODFToolkit(String filepath) {
        boolean absPath = false;

        return  absPath;
    }
}
